// The file contents for the current environment will overwrite these during build.
// The build system defaults to the dev environment which uses `environment.ts`, but if you do
// `ng build --env=prod` then `environment.prod.ts` will be used instead.
// The list of which env maps to which file can be found in `.angular-cli.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCEbvveB2Bn-zSpKaq7rxefwCOW0skUbhk",
    authDomain: "career-zone.firebaseapp.com",
    databaseURL: "https://career-zone.firebaseio.com",
    projectId: "career-zone",
    storageBucket: "career-zone.appspot.com",
    messagingSenderId: "961093340080",
    appId: "1:961093340080:web:d8fb96c94d617c80"
  }
};
